import { ItemDetailsProps } from "../constants/interfaces";
export declare const ItemDetails: (<T extends Record<string, any>>({ item, shownFields, header, important, prices, showMore, itemMap, related, }: ItemDetailsProps<T>) => import("react/jsx-runtime").JSX.Element) & {
    displayName: string;
};
